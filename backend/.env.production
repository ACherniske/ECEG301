# Production Environment Configuration
# Copy this file to .env and update with your production values

# Server Configuration
PORT=3000
NODE_ENV=production
FRONTEND_URL=https://your-frontend-domain.com

# JWT Configuration - IMPORTANT: Use a secure secret in production
JWT_SECRET=REPLACE-WITH-YOUR-SUPER-SECURE-JWT-SECRET-AT-LEAST-64-CHARACTERS-LONG-WITH-NUMBERS-AND-SPECIAL-CHARACTERS
JWT_EXPIRES_IN=24h

# Email Configuration
EMAIL_USER=your-production-email@gmail.com
EMAIL_APP_PASSWORD=your-gmail-app-password-16-characters

# Google Sheets Configuration
# These are automatically loaded from the service account JSON file
# But you can override them if needed:
# GOOGLE_SHEETS_PRIVATE_KEY=
# GOOGLE_SHEETS_CLIENT_EMAIL=

# Optional: Database URL if you decide to migrate from Google Sheets
# DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Redis URL for session storage
# REDIS_URL=redis://host:port

# AWS Configuration (for Lambda deployment)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

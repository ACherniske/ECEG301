# Production Environment Variables for AWS Deployment
# Copy this file to backend/.env for production deployment

# ===================================
# SERVER CONFIGURATION
# ===================================
PORT=8080
NODE_ENV=production

# ===================================
# FRONTEND CONFIGURATION
# ===================================
# Your GitHub Pages URL
FRONTEND_URL=frontend.com

# ===================================
# JWT CONFIGURATION
# ===================================
# IMPORTANT: Generate a secure random string (minimum 64 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_SECURE_64_CHARACTER_SECRET_STRING
JWT_EXPIRES_IN=24h

# ===================================
# EMAIL CONFIGURATION  
# ===================================
# Gmail configuration for sending emails
EMAIL_USER=your-production-email@gmail.com
EMAIL_APP_PASSWORD=your-gmail-app-specific-password

# ===================================
# GOOGLE SHEETS CONFIGURATION
# ===================================
# The ServiceAccount.json file will be used automatically
# Make sure it's in the backend/ directory and has proper permissions

# ===================================
# DEPLOYMENT NOTES
# ===================================
# 1. Replace ALL placeholder values above with real production values
# 2. Never commit this file with real secrets to version control
# 3. Use 'eb setenv' commands to set these in AWS Elastic Beanstalk
# 4. Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 5. Get EMAIL_APP_PASSWORD from your Gmail App Passwords settings

# ===================================
# EXAMPLE EB SETENV COMMANDS
# ===================================
# eb setenv NODE_ENV=production
# eb setenv FRONTEND_URL=https://acherniske.github.io
# eb setenv JWT_SECRET=your-64-character-secret
# eb setenv JWT_EXPIRES_IN=24h
# eb setenv EMAIL_USER=your-email@gmail.com
# eb setenv EMAIL_APP_PASSWORD=your-app-password

# GitHub Actions workflow disabled - using gh-pages branch deployment instead
# To re-enable GitHub Actions deployment, uncomment the code below

# name: Deploy Frontend to GitHub Pages
# 
# on:
#   push:
#     branches: [ main ]
#     paths: [ 'frontend/provider-portal/**' ]
#   workflow_dispatch:
# 
# permissions:
#   contents: read
#   pages: write
#   id-token: write
# 
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'
#         cache: 'npm'
#         cache-dependency-path: frontend/provider-portal/package-lock.json
#     - name: Install dependencies
#       run: |
#         cd frontend/provider-portal
#         npm ci
#     - name: Build application
#       run: |
#         cd frontend/provider-portal
#         npm run build
#     - name: Setup Pages
#       uses: actions/configure-pages@v4
#     - name: Upload artifact
#       uses: actions/upload-pages-artifact@v3
#       with:
#         path: frontend/provider-portal/dist
# 
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
